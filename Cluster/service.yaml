apiVersion: v1
kind: Service
metadata:
  name: argocd-image-updater
spec:
  selector:
    app: image-updater
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30008
  type: NodePort
